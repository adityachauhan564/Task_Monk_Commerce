# Task_Monk_Commerce
RESTful Coupons Management API built with Spring Boot. Supports Cart-wise, Product-wise and BXGY discount logic with extensible strategy-based design. Implements CRUD operations, dynamic discount calculation and MySQL persistence.
